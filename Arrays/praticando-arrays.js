const GPUS = ['GTX 1050TI', 'GTX 1060TI', 'RTX2060', 'RTX3050'];
const valores = [1500, 1800, 2600, 3000];

// GPUS.splice(1, 1);
// GPUS.push('GT 1030');
// let posicao = GPUS.indexOf('RTX3050');
// let posicao = valores.indexOf(3000);

// // console.log(GPUS);
// console.log(posicao);

// GPUS.push('RX560', 'RX580');

// console.log(GPUS);

// const lista1 = GPUS.slice(0, GPUS.length/2);
// const lista2 = GPUS.slice(GPUS.length/2);

// console.log(`GPUS da lista1 ${lista1}, GPUS da lista2 ${lista2}`);

// const copiar = GPUS.slice();

// console.log(copiar)

// GPUS.splice(1, 1);
// valores.splice(1, 1);

GPUS.push('GTX550');
valores.push()



console.log(GPUS);

const GPUSePrecos = [GPUS, valores];



// console.log(GPUSePrecos);